export const metadata = {
  title: 'Convert Signature to PNG Tool',
  description: 'Upload your signature and remove the white background instantly. 100% free with no registration required.',
};

export default function Layout({ children }) {
  return children;
}
